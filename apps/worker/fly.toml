app = "async-image-optimizer-worker"
primary_region = "fra"

[build]
  dockerfile = "apps/worker/Dockerfile"

[env]
  NODE_ENV = "production"
  WORKER_CONCURRENCY = "1"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
  cpus = 1
